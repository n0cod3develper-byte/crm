# ─── BACKEND CONFIG ──────────────────────────────────────────
PORT=4000
NODE_ENV=development

# Database (PostgreSQL)
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cargar_crm

# Cache (Segundos)
PERMISSIONS_CACHE_TTL=3600

# ─── FRONTEND CONFIG ─────────────────────────────────────────
# (Ubicado habitualmente en frontend/.env)
VITE_API_URL=http://localhost:4000/api/v1
