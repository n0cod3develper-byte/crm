import React from 'react';

export function TaskFiltersBar({ search, setSearch, filterStatus, setFilterStatus, currentView, setCurrentView }) {
  return (
    <div style={{ marginBottom: '1rem', padding: '1rem', border: '1px dashed #ccc' }}>
      <p>Filtros de Tareas (Mock)</p>
    </div>
  );
}
